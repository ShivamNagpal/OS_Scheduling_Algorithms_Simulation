struct VisualSection
{
	int startX, startY;
	int width, height;

	VisualSection(int startX, int startY, int width, int height)
	{
		this->startX = startX;
		this->startY = startY;
		this->width = width;
		this->height = height;
	}
};
